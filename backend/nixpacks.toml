# Railway uses Nixpacks for building Node.js apps
# This configuration ensures your backend deploys correctly

[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['echo "No build step required"']

[start]
cmd = 'npm start'

